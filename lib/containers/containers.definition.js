const containersDefinition =
`A container holds a Docker image with RAM and CPU
allocat, ports, and environment variables.

A component requires at least one container to run.`

export default containersDefinition
